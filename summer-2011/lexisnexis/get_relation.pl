#!/usr/bin/perl 
# this script can be used to test the relationship of two xml labels. 
# example: to test if headnote contains footnote we can do 

$externalnode = $ARGV[0]; 
$internalnode = $ARGV[1]; 

while (<STDIN>) {
    while (m/<$outernode>(.*?)</g) {
	
    }
}
